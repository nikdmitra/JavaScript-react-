import preloader from '../../../asets/images/tenor.gif'
import React from "react";

let Preloader = (props) => {
    return(
        
        <img src={preloader} />
    )

}
export default Preloader;